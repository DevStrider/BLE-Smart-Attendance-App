name: ble_smart_attendance_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2
dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.10.0
  cupertino_icons: ^1.0.8
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  firebase_database: ^11.3.4
  google_fonts: ^6.2.1
  permission_handler: ^12.0.0+1
  intl: ^0.20.2


dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  async: ^2.13.0
  fake_async: ^1.3.3
  leak_tracker: ^10.0.9
  material_color_utilities: ^0.12.0
  vm_service: ^15.0.0

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true